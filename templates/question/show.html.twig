{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ question }}</h1>

    <div>
        buenas buena
    </div>

    <h2>Answer ({{ answers|length }})</h2>

    <ul>
        {% for answer in answers %}
            <li>{{ answer }}</li>
        {% endfor %}
    </ul>
{% endblock %}